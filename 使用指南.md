# OfferPilot Plugin：介绍与使用指南

## 1. 插件介绍

OfferPilot 是一个纯前端 Chrome 网申辅助扩展。它在浏览器内维护个人档案、解析简历，并调用 OpenAI Responses 兼容服务把档案事实匹配到招聘表单。所有建议都需要用户确认，插件不会自动提交申请。

![OfferPilot 个人档案桌面预览](dist/offerpilot-profile-desktop.png)

## 2. 使用前准备

- Google Chrome。
- 一个由 OpenAI 或 Responses 兼容服务提供、仅供个人使用且未泄露的 API Key。
- 本项目目录：`/Users/zlr/Desktop/OfferPilot`。

不要复用曾在聊天、截图或源码中暴露过的 Key，也不要把共享 Key 打包进扩展。

## 3. 加载扩展

1. 在 Chrome 地址栏打开 `chrome://extensions/`。
2. 打开右上角“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择 `/Users/zlr/Desktop/OfferPilot/extension`。
5. 确认列表中出现“OfferPilot”，并将它固定到工具栏。

源码更新后，在扩展卡片上点击“重新加载”。Chrome 不能直接加载 ZIP；[offerpilot-plugin.zip](dist/offerpilot-plugin.zip) 需要先解压。

## 4. 配置 AI Agent

在扩展详情中打开“扩展程序选项”，填写：

1. **Agent API Key**：输入服务商提供的 Key，默认保持遮罩。
2. **API Base URL**：默认 `https://api.ai.tosky.top/v1`；也可填写其他 API 版本根路径，末尾不要填写 `/responses`。
3. **模型**：填写服务商支持的模型 ID，默认 `gpt-5.6-sol`。
4. **推理强度**：默认 `low`。
5. 点击“检测连接”，并允许 Chrome 访问该 API 域名。
6. 点击“保存档案”。

兼容服务必须实现 `GET /models`、`POST /responses` 和 Structured Outputs。只支持 `/chat/completions` 的服务目前不能使用。

API Key 和 Base URL 保存在当前 Chrome 配置文件的扩展存储中，不会进入网页、导出 JSON 或扩展源码。纯前端方案仍不等同于服务端密钥托管，仅适合个人本地使用。

## 5. 解析简历

点击右上角“解析简历”，选择 PDF、DOCX、HTML 或 HTM 文件。文件上限为 10 MB；PDF 必须包含可选择文字，纯扫描图片 PDF 暂不支持 OCR。

简历解析最长等待 4 分钟。中转服务较慢时建议将“推理强度”设为 `low`，再重新解析。

处理流程：

1. 扩展在浏览器本地提取文件文字，原始文件不会发送给 OpenAI。
2. 提取出的简历文字通过配置的 Responses 兼容 API 解析为结构化档案。
3. 页面展示姓名、联系方式、教育、实习、项目和技能摘要。
4. 点击“合并到档案”后，已有字段不会被覆盖。
5. 相同学校、公司或项目只补充缺失内容，新经历才会追加。

合并后应逐项检查日期、分类、联系方式和敏感字段。

## 6. 手动维护与备份

个人档案包括基础信息、求职意向、教育、实习、项目、校园、奖项、技能证书、自我评价和补充信息。

- “导入”支持 OfferPilot JSON，以及进入补充信息的 TXT/Markdown。
- “导出”生成结构化 JSON，不包含 API Key 和编辑器内部 ID。
- 输入停止约 1 秒后会自动保存，也可以点击“保存档案”。
- 证件号码、政治面貌、薪资等信息可以留空。

## 7. 填写招聘页面

1. 登录招聘网站并进入简历编辑或申请表单。
2. 点击 Chrome 工具栏中的 OfferPilot。
3. 点击“分析当前页面”。
4. 检查建议值、来源、置信度和确认状态。
5. 勾选正确项目，点击“应用选中”。
6. 检查最终页面，由你手动保存、进入下一步或提交。

插件只发送字段标签、类型、候选项和当前值，不发送整页 HTML。已有值默认不会覆盖，协议、声明、验证码和附件上传不会自动处理。

## 8. 数据与安全

- 档案和 API 设置保存在 `chrome.storage.local`。
- 存储访问被限制为扩展受信任上下文；招聘网页无法直接读取。
- 原始 PDF/DOCX/HTML 文件只在浏览器内存中处理。
- 简历文字和表单字段元数据会发送给配置的 Agent API。
- Agent 请求使用 Structured Outputs，并设置 `store: false`。
- 卸载扩展会删除本地档案和 API 设置，建议定期导出备份。

## 9. 常见问题

### 没有“加载已解压的扩展程序”

确认地址栏为 `chrome://extensions/`，再开启右上角“开发者模式”。文件选择窗口按 `Command + Shift + G`，输入 `/Users/zlr/Desktop/OfferPilot/extension`。

### 检测连接失败

检查 API Key、Base URL 和模型名称是否正确，确认服务支持 `GET /models` 与 Responses API，并查看扩展卡片中的 Service Worker 控制台错误。Base URL 应包含服务要求的 `/v1` 等版本路径，但不要包含 `/responses`。

### 简历解析失败

确认文件不超过 10 MB。扫描版 PDF 需要先通过 OCR 转为包含文字的 PDF；旧版 `.doc` 不受支持，应另存为 `.docx`。

### 某些字段没有匹配

常见原因是档案中缺少事实、字段已有值、页面标签不清晰、候选项不兼容，或 Agent 置信度不足。不要为了提高匹配率编造信息。

### 可以自动向招聘网站上传附件吗

不可以。“解析简历”只用于完善 OfferPilot 档案；招聘网站的简历、成绩单和证件照仍需手动上传。

## 10. 开发与检查

```bash
npm install
npm test
npm run check
npm run build:extension
```

打包结果输出到 `dist/offerpilot-plugin.zip`。项目不需要 `.env`、Express 后端或本地端口。
